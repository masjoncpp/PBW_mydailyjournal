# Disable directory listing
Options -Indexes

# Set default charset
AddDefaultCharset UTF-8

# Note: RewriteEngine requires mod_rewrite to be enabled
# If mod_rewrite is not enabled, comment out the lines below
# RewriteEngine On
# RewriteBase /

# If the request is not an existing file or directory, forward it to index.php
# RewriteCond %{REQUEST_FILENAME} !-f
# RewriteCond %{REQUEST_FILENAME} !-d
# RewriteRule ^(.*)$ index.php [L,QSA]

# Optional custom error pages (create these files if needed)
# ErrorDocument 404 /404.html
# ErrorDocument 500 /500.html
